# Prepare directories:
mkdir build
# Go in the 'build' directory
cd build
# Prepare the makefile
cmake ..
# Run the newly created makefile:
make
# Go back at the installation root directory
cd .. 
# Type for instance:
build/TakeABreak -in data/toy_example_reads.fasta,data/toy_example_with_inv_reads.fasta


# More details are in doc/user_manual_TakeABreak.txt 
 
